# Use the official Grafana image as the base
FROM grafana/grafana:latest

# Expose port 3000 for the Grafana web interface
EXPOSE 3001

# Optional: Set custom environment variables for Grafana login
ENV GF_SECURITY_ADMIN_USER=admin_user
ENV GF_SECURITY_ADMIN_PASSWORD=admin_pass

